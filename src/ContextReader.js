import Glob from 'glob';
import Path from 'path';
import FS from 'fs';

export default class ContextReader {
  static readContexts(paths){
    const context = {};

    return context;
  }

  constructor() {
  }
}
